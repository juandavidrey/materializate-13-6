<%= form_for([@test.course, @test]) do |f| %>
  <% if test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test.errors.count, "error") %>Prohibido esta prueba de ser guardado:</h2>

      <ul>
      <% test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Título" %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
	<%= f.submit %>
  </div>
<% end %>
